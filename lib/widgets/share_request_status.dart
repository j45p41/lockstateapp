import 'package:flutter/material.dart';

class ShareRequestStatus extends StatelessWidget {
  final String roomId;

  const ShareRequestStatus({Key? key, required this.roomId}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
} 